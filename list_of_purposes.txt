Редактор:

1. Кисти в отдельных окошках. Уменьшенные копии объектов в качестве иконок.
Окошки выбираются в меню галочками.
    1) Осязаемые объекты (шкафы, сундуки, кадки с цветами)
    2) Бэкграунд-объекты (ковры) TODO
    3) Стены разной длины
    4) Тайлы (?)
    5) NPC
2. Объект движется за курсором. DONE (Возможно, по сетке) TODO
3. Возможность ставить объекты на карту.
        > Подумать над тем, как сделать картины и прочие украшения, которые можно вешать на стены.
         (Возможная реализация: rect толщиной в один пиксель + прозрачные текстуры под картиной) TODO
4. Конвертер из render_list в json-карту
5. Удаление объектов. (ПКМ на объект > выпадающее меню > Удалить) TODO Реализовать меню с помощью tkinter'а
6. Перемещение объектов. (ПКМ на объект > выпадающее меню > Переместить)
7. Прокрутка карты.
8. Возможность задать объекту какую-либо функцию, активируемую при взаимодействии, и её параметры.
(ПКМ на объект > выпадающее меню > Функция)
9. Написать универсальные функции для продавцов, сундуков и говорящих NPC.


Основное:

1. Написать классы для предметов инвентаря.
2. Разобраться с классами объектов в описании.
3. Написать подкласс для сундука:
    1) Список предметов, лежащих внутри
    2) Метод, вызывающий класс Looting, переносящий предметы в инвентарь, удаляющий их из сундука и
    делающий его неактивным для взаимодействия
4. Разместить сундук на карте, прописывая в "arguments" список предметов.


Арт:

1. Нарисовать несколько скриншотов того, как должен выглядеть редактор карт.
2. Нарисовать картинки для разных видов стен.
